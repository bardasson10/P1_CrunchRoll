import { StyleSheet } from "react-native";


export const ButtonStyle = StyleSheet.create({
    buttonForm: {
        padding: 20,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    },
});